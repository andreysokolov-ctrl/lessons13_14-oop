# Вариант 25

| Файл | Задание |
|------|---------|
| `a.cpp` | Однонаправленный список: `+`, `--`, `!=` |
| `b.cpp` | Микроорганизмы: виртуальные функции, статические члены |
| `c.cpp` | Шаблонный список `TList<T>` |

```bash
g++ -std=c++17 -o a a.cpp && ./a
g++ -std=c++17 -o b b.cpp && ./b
g++ -std=c++17 -o c c.cpp && ./c
```
